{
  "tenantId": "${tenant_id}",
  "clientSecret": "${client_secret}",
  "scope": "${scope}",
  "clientId": "${client_id}",
  "senderId": "${sender_id}",

  "lwa_endpoint": "${lwa_endpoint}",
  "lwa_env": "${lwa_env}",
  "sor_endpoint": "${sor_endpoint}",
  "domain": "${domain}",
  "subdomain": "${subdomain}",
  "appName": "${app_name}",
  "gtin_persistence": "${gtin_persistence}"
}